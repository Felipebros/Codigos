name: label WIP checker CI

on:
  pull_request:
    types: [labeled, unlabeled, edited, opened, synchronize, reopened]

jobs:
  label-wip-checker-CI:
    runs-on: ubuntu-latest

    steps:
    - uses: Felipebros/github-action-WIP-blocker-with-labels-in-PR@1.0.0-beta
